{{- $id := .personal.git -}}
{{- if .is_work }}{{ $id = .work.git }}{{ end -}}
[user]
	name = {{ $id.name | quote }}
	email = {{ $id.email | quote }}

[init]
	defaultBranch = main
[pull]
	rebase = true
[core]
	autocrlf = input
